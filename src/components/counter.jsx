import React, { Component } from 'react';
class Counter extends Component {
    // state = {  }
    render() { 
        return <h1>Hello World</h1>; // when rendered returns an h1 element
    }
}
 
export default Counter; // can also be written in the class declaration